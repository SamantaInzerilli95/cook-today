{
  "name": "cook-today-server",
  "version": "1.0.0",
  "description": "Backend server for Cook Today! application",
  "main": "index.js",
  "scripts": {
    "build": "cd ../client && npm install && npm run build",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.19.2"
  }
}
